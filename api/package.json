{
  "name": "api",
  "version": "0.1.0",
  "description": "<server> npm i\r <client> npm i\r <client> npm run build\r <server> npm run prod",
  "main": "server-dev.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm --prefix ../app run build && node server.js",
    "dev": "nodemon server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "vite": "^6.0.3"
  }
}
